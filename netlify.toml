[build]
  command = "rm -rf node_modules package-lock.json && npm install --no-package-lock --include=dev && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
